# Project Structure

## Directories

- `config/`: The 
- `dataset/`: Dataset provider for helping with Datasets
- `detectors/`: Detector classes for the different safety classifiers
- `models/`: The internal probes
- `utils`:  Small helper scripts

## Files

- `run_safe_llm.py`: A script that runs inference through the whole Safety LLM pipeline
- `dataset/`: The Safety LLM Harness main class. This is where the magic happens!